
pipeline {
    agent any
    stages {
        stage('corpdmz.staples.com') {
            steps {
                echo "copying from corpdmz.staples.com"
                withCredentials([usernamePassword(credentialsId:'test',usernameVariable:'root',passwordVariable:'12345')]){
                sh 'sftp root@corpdmz.staples.com:/tmp/test1.tar ${WORKSPACE}/test2.tar'
            }
            }
        }    
    }
}
